# project dsh桌面应用 技能

## 用途

把 DeepSeek Harness 作为桌面 EXE 双击启动：Electron 壳负责拉起本机 DSH Web 服务，并加载带 token 的本地页面。

## 当前入口

- 桌面入口：`C:\Users\lwz12\Desktop\DeepSeek-Harness.exe`
- 项目根：`C:\Users\lwz12\Desktop\AI工作台知识库\01-项目\project dsh桌面应用`
- 用户数据：`%USERPROFILE%\.dsh`，修壳或打包时不要删除。
- 应用日志：`%APPDATA%\DeepSeek Harness\logs\app.log`
- 服务日志：`%APPDATA%\DeepSeek Harness\logs\service-*.log`

## 当前启动规则

- `config.json` 固定 DSH 版本为 `0.1.5-alpha.2`。
- `dshCommand` 指向 `%LOCALAPPDATA%\pnpm\bin\dsh.cmd`。
- `nodeExe` 指向已验证的 WinGet Node：`C:\Users\lwz12\AppData\Local\Microsoft\WinGet\Packages\OpenJS.NodeJS_Microsoft.Winget.Source_8wekyb3d8bbwe\node-v26.7.0-win-x64\node.exe`。
- 桌面壳会从 `dsh.cmd` 解析真实 `@deepseek-ai/dsh\lib\bin.js`，优先用 `nodeExe + bin.js` 直接启动服务，避免 Windows `cmd /c` 转义导致 `code=1`。
- 后端启动时默认注入 `NODE_OPTIONS=--use-system-ca`，让 Node 信任 Windows 系统证书库；本机代理证书导致 DeepSeek 请求 `SELF_SIGNED_CERT_IN_CHAIN` 时不要换 key，先验证这个选项。
- 服务输出里的 `dsh web: http://127.0.0.1:3080/?token=...` 必须进入最新 `service-*.log`，桌面壳靠它加载页面。
- `stopServiceOnExit` 默认为 `false`：关闭桌面窗口时保留本机 DSH 后端，下一次打开复用现有服务，减少启动链不稳定。
- 窗口加载前会先探测 token URL 是否 HTTP 可用；如果 Electron 首次加载失败，会每 5 秒继续重试，避免停在加载页不再恢复。

## 常用验证

```powershell
node --check main.js
npm run pack
```

冷启动验证：

```powershell
Start-Process -FilePath 'C:\Users\lwz12\Desktop\DeepSeek-Harness.exe'
netstat -ano | findstr ':3080'
```

再读最新 `service-*.log`，确认有 `?token=` URL；用 `Invoke-WebRequest` 请求 token URL，应返回 `STATUS=200` 且标题为 `DeepSeek Harness`。

## 常见坑

- 裸 `http://127.0.0.1:3080/` 返回 `401 Unauthorized` 是正常现象，新版 DSH 要用 token URL。
- 只看到桌面壳进程不代表后端服务已启动；必须同时检查 3080 监听、node 子进程和最新服务日志。
- 不要手写 `cmd.exe /s /c "\"...\dsh.cmd\" ..."` 这类命令行；Electron/Node 可能把引号变成字面量 `\"`，导致 Windows 报“不是内部或外部命令”。
- 若 PowerShell 手动运行正常但双击失败，先检查 `config.json` 的 `nodeExe` 是否还存在。
- 若界面里模型请求一直 `TRANSPORT` 重试，先用同一 Node 跑最小 DeepSeek 请求；若未加 `--use-system-ca` 时失败为 `SELF_SIGNED_CERT_IN_CHAIN`、加上后成功，说明是 Node 证书库问题，不是 API key。
